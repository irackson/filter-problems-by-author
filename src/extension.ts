import * as vscode from 'vscode';
import * as path from 'path';

const extensionName = 'filter-problems-by-author';

export function activate(context: vscode.ExtensionContext) {
  // context.subscriptions.push(
  //     vscode.commands.registerCommand(`${extensionName}.mytest`, () => {
  //         copyRelativePathAndTriggerNpmScript('test');
  //     })
  // );
  // context.subscriptions.push(
  //     vscode.commands.registerCommand(`${extensionName}.mysync`, () => {
  //         copyRelativePathAndTriggerNpmScript('sync');
  //     })
  // );
}

export function deactivate() {}
